#include "Complex.h"
#include "Fraction.h"
#include "Quadro.h"

int main() {


    Fraction f1 = Fraction(0);
    Fraction f2 = Fraction(-1);
    Fraction f3 = Fraction(-1);

    Quadro(f1,f2,f3);

    return 0;
}